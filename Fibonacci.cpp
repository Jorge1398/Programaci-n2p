using namespace std;
#include<iostream>

void fibonaci(int x);

int main()
{
	int x;
	cout<<"Ingrese la cantidad de terminos: ";
	cin>>x;
	fibonaci(x);
	return 0;
}

void fibonaci(int x)
{
	if(int x==1)
	   return 0;
	else
	(if==2)
	   return==1;
	else
	   return fibonaci(x-1)+(x-2);
}
